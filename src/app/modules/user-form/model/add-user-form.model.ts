export interface UserFormRequestModel {
  name: string;
  email: string;
  phoneNumber: string;
  address: string;
}

export interface UserFormsList {
  id: number;
  name: string;
  email: string;
  phoneNumber: string;
  address: string;
}